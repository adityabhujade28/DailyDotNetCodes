using System;

namespace TicTacToe.Api.Auth
{
    public class JwtTokenGenerator
    {
        // Placeholder - replace with real JWT generation when integrating JWT
        public string GenerateToken(Guid userId) => userId.ToString();
    }
}